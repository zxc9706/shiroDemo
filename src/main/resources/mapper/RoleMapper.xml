<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxc.demo.mapper.PermissionMapper">
    <!--根据userId查询角色信息-->
    <select id="findRoleById" parameterType="Integer" resultType="com.zxc.demo.Model.Role">
        select r.id, r.role
        from sys_roles r
        left join sys_users_roles ur on ur.role_id = r.id
        left join sys_users u on u.id = ur.user_id
        where 1=1
        and u.id = #{id}
    </select>

</mapper>